<h1>Edit Line Item for Invoice <%= @invoice.id %></h1>

<% form_for [@invoice, @line_item] do |f| %>
  <%= render :partial => 'form', :locals => { :f => f } %>
  <%= f.submit "Update Line Item" %>
<% end %>