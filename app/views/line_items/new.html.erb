<h2>New Line Item for Invoice <%= @invoice.id %></h2>

<% form_for [@invoice, @line_item] do |f| %>
  <%= f.error_messages %>
  
  <%= render :partial => 'form', :locals => { :f => f } %>
  <%= f.submit "Create Line Item" %> or <%= link_to "Cancel", :back %>
<% end %>