<h1>New Line Item for Invoice <%= @invoice.id %></h1>

<% form_for [@invoice, @line_item] do |f| %>
  <%= render :partial => 'form', :locals => { :f => f } %>
  <%= f.submit "Create Line Item" %>
<% end %>